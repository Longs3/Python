#!/usr/bin/python3
"""
This is a temporary code for testing and running small code
chmod is already added


dir(__builtins__) to find all the built-ins function
dir(math)  to find out more about the modules
help(func) to find out more about the function

pip install [package name]
pip install [package name]==x.x.x
pip list --outdated
pip install [package name] --upgrade
pip install -U pip

"""
#code start here
import datetime
now = datetime.datetime.now()
print(repr(now))
